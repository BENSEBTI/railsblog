<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </p>
  <p>
    <%= f.label :commentaire %><br>
    <%= f.cktext_area :commentaire %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>